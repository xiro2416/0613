# 模块五：生活服务 Skills 连接器

> 核心定位：高压决策后的生理补给站、认知无载荷行动链

## 接口状态 ✅

- 文件：`backend/modules/life_service.py`
- 类：`LifeServiceConnector(BaseModule)`
- 模块名：`life_service`
- 方法：`process(context: ModuleContext) -> ModuleResult`

## 人设路由策略

| 人设 | 激活方式 | 服务卡片风格 |
|---|---|---|
| GENTLE | 非侵入式触发 | 治愈关怀风 |
| RATIONAL | 非侵入式触发 | 极简效率风 |
| MEME | 非侵入式触发 | 轻松活泼风 |

触发条件：仅在识别到疲劳、熬夜、高压状态时激活，非主动对话模式。

## 核心职责（PRD 定义）

1. 识别用户疲劳、熬夜、高压状态为次生交易风险
2. 内嵌第三方生活服务技能（核心适配瑞幸咖啡等补给场景）
3. 智能触发轻量化服务卡片
4. 支持界面内一键完成消费、补给操作
5. 实现无感知生理后勤保障

## 待定功能细节

1. 疲劳/高压状态的识别方式（用户自述 vs 使用时长 vs 时间推断）？
2. 第三方服务对接方式（API 内嵌 vs 跳转小程序 vs DeepLink）？
3. 初期接入哪些服务（仅瑞幸 vs 多服务）？
4. 服务卡片的触发频率上限（避免骚扰）？
5. 支付流程在系统内完成还是跳转？

## 下次工作入口

1. 确认疲劳识别规则和触发阈值
2. 确定第三方服务的对接方案
3. 定义服务卡片的数据结构和交互协议
